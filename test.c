#include <stdio.h>

int main(int argc, char const *argv[])
{
    int i;

    printf("Full name : %s %s\n", argv[1], argv[2]);

    // for (i = 0; i < argc; i++) 
    // {
    //     printf("argv[%d] = \"%s\";\n", i, argv[i]);
    // }
    return 0;
}
